# Structured Markdown Schema v1

Documents the `StructuredContent`, `Unit`, `Component`, and `Attribute` JSON Schema definitions that represent the Article -> Unit -> Component -> Attribute content hierarchy in v1.
