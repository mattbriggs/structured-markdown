# Architecture Overview

The structure parser is a layered pipeline that transforms raw source files into a normalized, schema-validated content model suitable for publishing, validation, and RAG ingestion.
