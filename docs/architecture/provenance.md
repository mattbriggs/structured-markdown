# Provenance

The `DocumentProvenance` contract records the content hash, parse timestamp, adapter version, and schema version for every parsed document to support cache invalidation and reproducible builds.
