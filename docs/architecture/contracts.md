# Contracts

All inter-layer data is exchanged via immutable Pydantic models defined in `src/structure_parser/contracts/`, providing a versioned, schema-serializable contract between every stage of the pipeline.
