# Diagnostics

Every issue detected during parsing is emitted as a `Diagnostic` with a stable `SP-NNN` code, severity, category, human-readable message, and remediation guidance so authors can act on feedback without reading source code.
