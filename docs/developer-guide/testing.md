# Testing Guide

The test suite is organized into unit, contract, and integration layers; fixtures live in `tests/fixtures/` and tools in `tools/` support fixture validation and expected-output regeneration.
